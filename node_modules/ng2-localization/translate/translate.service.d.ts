import { EventEmitter } from '@angular/core';
/**
 * Created by ali on 5/11/17.
 */
export declare class TranslateService {
    private _translations;
    private _defaultLocal;
    private _currentLocal;
    private _fallback;
    onLocalChanged: EventEmitter<string>;
    private PLACEHOLDER;
    constructor(_translations: any);
    getCurrentLocal(): string;
    getSupportedLocal(): string[];
    setDefaultLocal(local: string): void;
    enableFallback(enable: boolean): void;
    use(local: string): void;
    private translate(key);
    instant(key: string, words?: string | string[]): string;
    private replace(word?, words?);
}
